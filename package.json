{
  "name": "techswat-auth-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda auth function for API Gateway HTTP API v2",
  "main": "src/handler.js",
  "type": "commonjs",
  "scripts": {
    "dev": "node scripts/invoke.js",
    "dev:local": "cross-env ENV_FILE=.env.dev node scripts/invoke.js",
    "test": "jest"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "jest": "^29.7.0"
  }
}
